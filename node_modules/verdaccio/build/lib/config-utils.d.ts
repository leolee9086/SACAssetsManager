import { PackageList } from '@verdaccio/types';
/**
 * Normalize user list.
 * @return {Array}
 */
export declare function normalizeUserList(oldFormat: any, newFormat: any): any;
/**
 * Check whether an uplink can proxy
 */
export declare function hasProxyTo(pkg: string, upLink: string, packages: PackageList): boolean;
