import { Config } from '@verdaccio/types';
export declare function displayExperimentsInfoBox(flags: any): void;
/**
 * Exposes a server factory to be instantiated programmatically.
 *
    const app = await runServer(); // default configuration
    const app = await runServer('./config/config.yaml');
    const app = await runServer({ configuration });
    app.listen(4000, (event) => {
      // do something
    });
 * @param config
 */
export declare function runServer(config?: string): Promise<any>;
/**
 * Return a native HTTP/HTTPS server instance
 * @param config
 * @param addr
 * @param app
 */
export declare function createServerFactory(config: Config, addr: any, app: any): any;
