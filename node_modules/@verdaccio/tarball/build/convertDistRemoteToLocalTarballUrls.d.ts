import { Package, Version } from '@verdaccio/types';
import { RequestOptions } from '@verdaccio/url';
/**
 * Iterate a packages's versions and filter each original tarball url.
 * @param {*} pkg
 * @param {*} req
 * @param {*} config
 * @return {String} a filtered package
 */
export declare function convertDistRemoteToLocalTarballUrls(pkg: Package, request: RequestOptions, urlPrefix: string | void): Package;
/**
 * Convert single Version disst tarball
 * @param name
 * @param version
 * @param request
 * @param urlPrefix
 * @returns
 */
export declare function convertDistVersionToLocalTarballsUrl(name: any, version: Version, request: any, urlPrefix: any): Version;
