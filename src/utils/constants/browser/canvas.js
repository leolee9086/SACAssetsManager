// 这些混合模式决定了新绘制的图形如何与已有的画布内容进行混合计算
export const $canvas混合模式 = {
    // 基础操作类 - 用于基本的图层合成操作
    $清除: 'clear',          // 完全清除目标区域，使其变为透明（alpha = 0）
    $源图: 'copy',           // 修改：从 'source' 改为 'copy'
    $覆盖: 'source-over',           // 标准混合模式：源图层绘制在目标图层上方，考虑透明度
    $在内: 'source-in',      // 修改：从 'in' 改为 'source-in'
    $在外: 'source-out',     // 修改：从 'out' 改为 'source-out'
    $顶部: 'source-atop',    // 修改：从 'atop' 改为 'source-atop'
    
    // 目标图优先类 - 以目标图层为主的合成操作
    $目标: 'destination',    // 修改：从 'dest' 改为 'destination'
    $目标覆盖: 'dest-over',   // 将目标图层置于源图层上方（与'over'相反）
    $目标在内: 'dest-in',     // 仅显示两图层重叠且源图层不透明的目标图层部分
    $目标在外: 'dest-out',    // 仅显示不与源图层重叠的目标图层部分
    $目标顶部: 'dest-atop',   // 在重叠区域显示目标图层，保留源图层不重叠的部分
    
    // 特殊混合类 - 用于特定的视觉效果
    $异或: 'xor',            // 显示不重叠区域，重叠区域变为透明
    $添加: 'add',            // 源图层与目标图层的颜色值相加，产生明亮的叠加效果
    $饱和: 'saturate',       // 提高目标图层的饱和度，基于源图层的饱和度值
    
    // 图像混合类 - 用于专业的图像合成效果
    $正片叠底: 'multiply',    // 逐像素相乘，深色更深，常用于阴影和纹理叠加
    $滤色: 'screen',         // 逐像素反相相乘，亮色更亮，适合光照和发光效果
    $叠加: 'overlay',        // 结合正片叠底和滤色，增强对比度和色彩鲜艳度
    $变暗: 'darken',         // 逐像素比较并保留较暗的颜色值，用于暗部处理
    $变亮: 'lighten',        // 逐像素比较并保留较亮的颜色值，用于高光处理
    
    // 色彩调整类 - 用于精细的色彩处理
    $颜色减淡: 'color-dodge', // 通过降低对比度提亮图像，创造明亮通透的效果
    $颜色加深: 'color-burn',  // 通过增加对比度加深图像，强化暗部细节
    $强光: 'hard-light',     // 根据源图层颜色应用正片叠底或滤色，产生强烈对比
    $柔光: 'soft-light',     // 柔和版的强光效果，产生自然的明暗过渡
    $色相: 'hue',            // 保留目标图层的亮度和饱和度，使用源图层的色相
    $饱和度: 'saturation',    // 保留目标图层的亮度和色相，使用源图层的饱和度
    $颜色: 'color',          // 保留目标图层的亮度，使用源图层的色相和饱和度
    $亮度: 'luminosity',     // 保留目标图层的色相和饱和度，使用源图层的亮度
    
    // 数学运算类 - 基于颜色值的数学运算
    $差值: 'difference',     // 计算颜色值之差的绝对值，常用于图像对比和边缘检测
    $排除: 'exclusion'       // 类似差值但对比度较低，产生柔和的反色效果
}
