/**
 * 常量定义统一导出
 * 
 * 本文件统一导出所有常量定义，用于简化导入路径
 * 
 * 开发指南：
 * 1. 避免在此文件中定义新常量，应在相应的常量文件中定义
 * 2. 此文件应只包含导入和导出语句
 */

// 导入所有常量模块
import { 错误码, 通用错误码, 文件错误码, 资源错误码, 网络错误码 } from './errorCodes.js';
import { API端点, 思源API端点, 插件API端点, 外部API端点 } from './apiEndpoints.js';
import { 
  事件类型, 资源事件类型, UI事件类型, 生命周期事件类型, events 
} from './eventTypes.js';
import { 
  UI常量, 面板类型, 面板位置, 对话框类型, 
  按钮类型, 主题类型, 视图模式, 排序方式 
} from './uiConstants.js';
import {
  浏览器常量, $插入位置, $选区位置, $滚动行为, $可见性, $拖拽效果,
  $编辑模式, $输入模式, $缓存策略, $通知方向, $观察类型, $文本对齐,
  $字体平滑, $换行模式, $背景混合
} from './browserConstants.js';

// 统一导出所有常量
export {
  // 错误码相关常量
  错误码, 通用错误码, 文件错误码, 资源错误码, 网络错误码,
  
  // API端点相关常量
  API端点, 思源API端点, 插件API端点, 外部API端点,
  
  // 事件类型相关常量
  事件类型, 资源事件类型, UI事件类型, 生命周期事件类型, events,
  
  // UI相关常量
  UI常量, 面板类型, 面板位置, 对话框类型, 按钮类型, 主题类型, 视图模式, 排序方式,
  
  // 浏览器相关常量
  浏览器常量, $插入位置, $选区位置, $滚动行为, $可见性, $拖拽效果,
  $编辑模式, $输入模式, $缓存策略, $通知方向, $观察类型, $文本对齐,
  $字体平滑, $换行模式, $背景混合
}; 